services:
  - type: web
    name: agent-zero
    env: python
    pythonVersion: 3.12.10  # compatible with kokoro and other deps
    buildCommand: pip install --no-cache-dir -r requirements.txt
    startCommand: python run_ui.py
